# TF-IDF Model Configuration

# Model parameters
use_abstract: true
title_field: title
abstract_field: abstract

# Training (not used for TF-IDF - it's a non-trainable model)
epochs: 0
train_batch_size: 2048
eval_batch_size: 4096
learning_rate: 0.001

# Evaluation
neg_sampling: null
