# TF-IDF Model Configuration

# Model parameters
use_abstract: true
title_field: title
abstract_field: abstract

# Training (not used for TF-IDF but required by RecBole)
epochs: 1
train_batch_size: 2048
eval_batch_size: 4096
learning_rate: 0.001

# Evaluation
neg_sampling: null
